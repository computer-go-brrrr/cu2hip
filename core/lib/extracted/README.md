# core/lib/extracted/ — Rocq extraction output (G4)

Everything here EXCEPT `dune` and this README is GENERATED by
`make -C rocq` (see `rocq/Extract.v`; output dir must exist before
running extraction — CI creates it with `mkdir -p`). Do not hand-edit
`*.ml`/`*.mli`; change the `.v` sources and re-extract instead.
